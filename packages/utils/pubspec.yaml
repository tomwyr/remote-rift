name: remote_rift_utils
description: Shared Dart utilities and common data models.
publish_to: none

resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  json_annotation: ^4.9.0
  equatable: ^2.0.8

dev_dependencies:
  test: ^1.25.6
  fake_async: ^1.3.3
  json_serializable: ^6.12.0
  build_runner: ^2.10.5
