name: remote_rift_mcp
description: Local Model Context Protocol server for Remote Rift Desktop.
publish_to: none

resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  mcp_dart: ^2.4.2
  remote_rift_core: any
