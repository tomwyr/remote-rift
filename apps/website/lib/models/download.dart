class const DownloadLink({final String? label, final String? icon});

class const DownloadOption({
  required final String title,
  required final String description,
  required final List<DownloadLink> links,
});
